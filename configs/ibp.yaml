defaults:
  - _self_
  - network: conv_small_cifar
  - dataset: cifar10
  - method: composer_ibp
  - exp: output_vs_input

fabric:
  _target_: lightning.Fabric
  num_nodes: 1
  devices: 1
  accelerator: cpu

wandb:
  entity: ${oc.env:WANDB_ENTITY}
  project: ${oc.env:WANDB_PROJECT}