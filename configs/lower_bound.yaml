defaults:
  - _self_
  - network: conv_small_mnist
  - dataset: mnist
  - method: composer_lower_bound
  - exp: get_output_bounds

fabric:
  _target_: lightning.Fabric
  num_nodes: 1
  devices: 1
  accelerator: cpu

wandb:
  entity: ${oc.env:WANDB_ENTITY}
  project: ${oc.env:WANDB_PROJECT}